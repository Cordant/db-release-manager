name: unique-name
stage: ${opt:stage}
client: ${opt:client, connect}
database:
  host: 'local-bituhh.ddns.net'
  username: 'postgres'
  password: '140496'
env:
  error:
    message: 'This is an error!'